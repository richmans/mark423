<p style="color: green"><%= notice %></p>

<h1><%=t(:podcasts)%></h1>

<div id="podcasts">
  <% @podcasts.each do |podcast| %>
    <%= render podcast %>
    <p>
      <%= link_to t(:model_show, model: Podcast), podcast %>
    </p>
  <% end %>
</div>

<%= link_to t(:model_new, model: Podcast), new_podcast_path %>
